# Bootstrap-Beginners-project-
this is a basic webpage that uses HTML for structure, CSS for styling, and JavaScript/jQuery for interactivity.CSS (for styling, some of which is handled through Bootstrap).
